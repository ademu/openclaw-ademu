// Full runtime entry of the Ademú channel plugin. Slice OPENCLAW-ADEMU-1, task T10 replaces this
// stub with `defineChannelPluginEntry(...)` from `openclaw/plugin-sdk/channel-core`; until then the
// package compiles and packs with an inert entry so the gates (T1) can run.
export {};
